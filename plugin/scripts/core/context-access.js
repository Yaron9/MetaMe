'use strict';

const {
  ACCESS_TRUSTS,
  accessIdentity,
  isTrustedAccess,
  normalizeAccessContext,
  resolveAccessContext,
} = require('./context-manifest');

module.exports = {
  ACCESS_TRUSTS,
  accessIdentity,
  isTrustedAccess,
  normalizeAccessContext,
  resolveAccessContext,
};
