# Private files excluded from public release
# One pattern per line (glob syntax, relative to repo root)

# User config (contains Feishu/Telegram tokens, chat IDs)
scripts/daemon.yaml

# Development notes (contains private repo URL, publish workflow)
x.md

# Symlinks to local agent files (broken + expose local paths)
MEMORY.md
SOUL.md
