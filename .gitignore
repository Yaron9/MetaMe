node_modules
.DS_Store
# Local QMD development install (users install globally via bun install -g github:tobi/qmd)
qmd/
CLAUDE.md
.claude_profile.yaml
ROADMAP_PHASE_B.md
METAME_MAINTENANCE.md
package-lock.json
# User private config — only daemon-default.yaml (template) is tracked
scripts/daemon.yaml
feishu-login-qr.png

# Personal/private files — never upload
MEMORY.md
SOUL.md
.env*
*.log

# Playwright browser profile (runtime data, never commit)
~/.metame/browser-profile/

# Playwright MCP console logs
.playwright-mcp/

# Team worktrees (auto-created per agent)
.worktree/

# Local working dirs (never push)
docs/
!scripts/docs/
task/
tasks/
upload/

# Generated test files
test_daemon.js
scripts/test_daemon.js
run_test.js
x.md

# Build artifacts
*.tgz

# Dev-only config (not needed by users)
eslint.config.js
__pycache__/
*.pyc
AGENTS.md
