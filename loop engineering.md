# 智能软件工程中的循环工程学与多后端外挂系统架构白皮书

## 循环工程学的定义与范式转移

在人工智能驱动的软件开发（AI-generated Software Engineering, AISE）演进中，业界正在经历一次从“提示词工程（Prompt Engineering）”向“循环工程（Loop Engineering）”的范式转移^^。传统的交互模式依赖于人类工程师编写提示词、获取单次响应并手动微调代码，这种“单次前向传导”在面对复杂的、多步骤的现实工程任务时很快就会遇到瓶颈^^。循环工程的核心思想在于，设计一种能够使智能体在无人类持续干预的情况下，自主执行、观察、评估并不断修正其生成结果的系统闭环^^。在这种新范式下，开发者的定位从编写单一提示词的“操作员”，转变为设计自主控制系统（Control Systems）的“系统架构师”^^。

从控制论的角度来看，循环工程借鉴了传统反馈控制系统的逻辑，即通过测量输出、将其与目标进行对比并动态调整输入^^。应用于代码智能体（Coding Agents）时，这一逻辑表现为微观上的“感知、推理、规划、行动、观察（Perceive, Reason, Plan, Act, Observe）”迭代链条^^。智能体首先感知环境状态，通过大型语言模型（LLM）进行推理，将顶层目标拆解为具体的行动步骤，随后调用工具执行写入、运行测试等具身动作，并捕获执行环境返回的控制台输出或错误日志^^。如果执行结果偏离预期，智能体将错误信息作为新的输入重新推理，从而在内部实现动态的策略自纠正^^。这种机制使智能体表现出类似于人类软件工程师的试错与调试行为^^。

## 可持续智能体循环的五个核心支柱与四种触发范式

构建一个在生产环境中具备生命力且可持续运转的循环系统，需要系统性的支撑结构。业界将一个完备的循环工程体系归纳为五个核心技术支柱，它们共同决定了系统能够运行多深以及如何安全地与外部现实世界交互^^。

### 循环工程的五个核心支柱

* **自动化引擎（Automations）** ：这是整个系统的运行底座，负责确保循环是一个可持续被唤醒、执行的程序，而不是一过性的脚本^^。它允许智能体在检测到外部状态变更时，自主进行任务发现、分发与分类^^。
* **物理工作树隔离（Worktrees）** ：为了避免并行任务之间的上下文冲突和代码污染，系统必须能够在隔离的、临时的物理分支或容器沙箱内运行智能体，支持多个智能体在不发生合并冲突的前提下并行推进工作^^。
* **轻量化技能库（Skills）** ：这是智能体执行任务的具体操作规程。通过将最佳实践、领域规范或高频工具调用封装为轻量化的可配置技能，智能体可以在运行时按需加载，而无须将所有知识锁死在静态提示词中^^。
* **插件与连接器（Plugins & Connectors）** ：外挂系统与外部生态（如 GitHub、Slack、Jira、各类 CI/CD 管道）进行数据交互和命令投递的通道，是智能体获取“具身”感知能力的基础^^。
* **状态与指标追踪系统（State Tracking & Telemetry）** ：用于记录循环运行轨迹的状态机。它能够实时监控智能体的运行 Turn 数、Token 损耗、执行时长以及财务预算，同时支持会话在异常中断后的反序列化恢复^^。

在触发层面，一个成熟的可持续循环系统不再依赖人类工程师敲击键盘来触发，而是通过四种主流的自动化触发范式来对智能体进行“拨动”，使其保持长效的主动服务状态^^。

### 智能体循环核心驱动原语与自动化触发范式对比

| **触发与原语范式**       | **运行机制与控制原理**                                                                       | **适用场景与典型工程实例**                                                               | **资源消耗与 Token 损耗特征**                                                  |
| ------------------------------ | -------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------ |
| **心跳触发 (Heartbeat)** | 定时唤醒机制，例如每隔 5 分钟自动启动一次后台进程，对指定资源执行状态探针^^。                      | 代码仓库日常巡检、未合并分支依赖冲突静态扫描、临时安全审计^^。                                 | 较低。单次执行时间极短，属于轻量级的轮询探针，通常仅在状态异常时拉起完整执行链^^。   |
| **定时任务 (Cron)**      | 按照标准的时钟调度规则（如特定周期的具体时刻）唤醒高权限智能体执行重度批量任务^^。                 | 每周五下午拉起 Codex 执行遗留 PR 的自动化代码质量审查、单元测试覆盖率补齐^^。                  | 中等。由于属于计划内唤醒，通常在非业务高峰时段集中爆发，消耗在预期内可控^^。         |
| **事件钩子 (Hooks)**     | 基于发布/订阅模型，当外部系统状态发生变更（如收到 Webhook 或内部生命周期事件触发）时即时唤醒^^。   | 新 Pull Request 创建时触发 Claude Code 执行自动合规性 Lint 审查，或新 Issue 创建时启动诊断^^。 | 波动性高。消耗完全取决于外部业务事件的频次，需要在外挂层设计严格的并发队列管理。     |
| **目标导向原语 (Goals)** | 设定最高终止状态边界（使用 `/goal`指令），由大模型自身驱动推理和工具链，迭代运行直至达成验证^^。 | 修复特定报错堆栈、升级库版本并保证 CI 绿灯、重构指定安全漏洞^^。                               | 极高。模型自主决策，容易因遭遇未知错误或逻辑死锁而产生无限回滚重试，需设置硬熔断^^。 |

## Claude Code 与 OpenAI Codex 的微观内核循环解析

要为 Claude Code 与 OpenAI Codex 升级外挂系统（Harness），必须深入剖析两者在模型级、SDK 级和应用服务器层面的微观内核循环设计。这决定了外挂层在何处可以与内核进行无缝的状态共享，以及何处存在底层协议的天然差异。

### Claude Code 的微观内核架构

Claude Code 的本质是一个围绕 Claude 系列模型构建的高性能 Agentic Harness，其核心执行层利用 TypeScript 编写的 `queryLoop()` 异步生成器来管理单轮 Turn 的上下文整合、工具分发以及事件流式输出^^。

#### 1. 单轮 Turn 执行逻辑与工具并发机制

在每次迭代中，大模型接收系统提示词、工具定义以及当前会话快照，决定是直接输出自然语言，还是请求一个或多个工具调用^^。Claude Code 实现了精细的工具并行与顺序执行逻辑：为了提高性能，所有被标记为只读的工具（如 `Read` 文件、`Glob` 检索、`Grep` 搜索）会通过并发通道（Concurrent Executer）在单轮内同时调用；而带有副作用的写工具或指令（如 `Edit` 编辑、`Write` 写入、`Bash` 指令）则强制按照声明顺序单线程串行执行，以防止物理环境产生不可预测的状态竞态^^。

#### 2. 五阶段上下文紧缩管线（Compaction Pipeline）

为应对长上下文带来的注意力分散与成本膨胀，Claude Code 建立了一套严密的紧缩机制^^。在每次发起 API 请求前，历史记录会流经五个相继触发的塑形器（Shapers）：首先根据剩余额度调减预算，其次对长调用日志进行行级剪枝（Snip），接着进行微观语法折叠，再对中间态转录进行折叠（Context Collapse），最后对已经达成目标的子 turn 进行自动归并（Auto-compact）^^。此外，它还通过 CLAUDE.md 等本地文件的层次化读取加载个性化与项目级的静态上下文，并利用 auto memory 机制持久化跨会话的学习成果^^。

#### 3. 侧链转录（Sidechain Transcripts）

当主进程需要派发复杂的深层探索任务时，它会通过内置的子智能体生成工具（`AgentTool`）克隆一个新的轻量化执行上下文^^。子智能体所有的探索和试错均在独立的物理存储文件（Sidechain Transcripts）中流转，这确保了子任务长达数十轮的杂乱历史不会回流，主上下文仅承载最终提炼出的 Summary^^。

### OpenAI Codex 的微观内核架构

与 Claude Code 客户端外壳式的轻量级设计不同，OpenAI 针对 Codex 体系的工程落地（如 OpenClaw 深度融合的运行时）采用了高度结构化、松耦合的应用服务器（App Server）架构，其状态管理与通信协议设计极其硬核^^。

#### 1. Rust 编写的持久化运行时内核（Codex Core）

Codex 的会话生命周期与底层通信完全封装在一个由 Rust 编写的本地库 `Codex core` 中^^。由于采用底层语言实现，它能够在系统底层直接劫持、拦截并沙箱化系统调用，提供比 Node.js 运行时更为坚固的安全底座^^。

#### 2. stdio 上的 JSON-RPC 应用服务器接口

外挂系统与底层 Codex 引擎的通信被完全解耦。它们之间通过标准输入输出（stdio）承载的双向 JSON-RPC 协议进行通信，称为 App Server^^。会话被解构为三个清晰的阶梯基元：原子输入输出单元（Item，包括 diff、审批请求、消息流）、工作处理轮次（Turn，从用户敲击指令到最终结果产出的一个完整生命周期，包含中间多次工具自循环）以及持久化存储容器（Thread，用于将全量事件序列化并保存至磁盘，防止网络中断或宿主进程崩溃导致状态丢失）^^。

#### 3. 严格分层的前缀缓存优化（Prefix Caching）

由于每一次重试都需要附带历史对话，导致整个网络传输成本与计算延迟呈现 **$O(n^2)$** 的膨胀趋势^^。为了最大化利用 OpenAI 服务端的前缀缓存（Prefix Caching）特性，Codex 强制对生成的 Prompt 进行分层拼装：最头部的 System Instructions（包括人格设定与静态工具 Schema）必须保持绝对静态；任何动态变更（如当前沙箱路径改变、环境变量调整）一律严禁直接修改或插入到 Prompt 头部，而是作为新生成的 developer 消息追加在会话序列的最末端^^。任何对头部的微小改动都会使缓存瞬间失效，而 Codex 通过这种尾部追加（Append-only）设计，锁定了高达 90% 以上的前缀缓存命中率^^。

#### 4. 模型原生隐空间紧缩（Model-Native Compaction）

在处理极度漫长的工作流时，Codex 的压缩机制并非依赖客户端进行文本摘要，而是直接调用服务端的 `/responses/compact` 专属端点^^。该端点不生成损耗严重的自然语言描述，而是计算出一个被加密的、包含模型隐藏状态（Latent States）的特殊二进制对象（`encrypted_content`）^^。对于注重隐私（Zero Data Retention, ZDR）的企业客户，OpenAI 在计算时仅保留该 Blob 的解密密钥^^。每次新 Turns 开启时，外挂将此解密后的状态流灌注回模型，从而在近乎零信息损耗的前提下恢复长对话中的深层推理上下文^^。

### Claude Code 与 OpenAI Codex 的内核设计特征对比

| **内核设计维度**           | **Claude Code 架构方案**                                          | **OpenAI Codex 架构方案**                                                        |
| -------------------------------- | ----------------------------------------------------------------------- | -------------------------------------------------------------------------------------- |
| **运行时底座与进程模型**   | 基于 TypeScript 开发的 Agent SDK，直接调用进程级别 CLI。                | 基于 Rust 编写的静态链接库 `Codex core`，通过 stdio 的双向 JSON-RPC 应用服务器运行。 |
| **上下文层级与长期记忆**   | 依托本地 `CLAUDE.md`目录树架构，配合用户级别 `auto memory`。        | 统一由服务端 Thread 存储结构托管，支持完全的本地/云端多端镜像投影。                    |
| **多流并发与工具执行安全** | 并行执行无副作用工具，对带有副作用的工具采用严格的顺序序列化机制。      | 物理沙箱层执行拦截，所有工具在专门的 Sandbox 隔离中并行运行。                          |
| **会话持久化与中断恢复**   | 基于 append-only 的 JSONL 文件进行 Session 状态追踪，支持 resume/fork。 | 原子级的 Item 级事件流。Turn 在执行中随时可抛出挂起事件，等待批准后继续。              |
| **缓存敏感度策略**         | 模型感官自适应，动态编译。                                              | 极度苛刻的前缀对齐设计，强制在尾部追加上下文。                                         |

## 外挂系统（Harness）架构设计：功能冗余度与必要建设项判定

对于专门针对 Claude Code 与 Codex 进行升级的外挂系统设计，必须要厘清智能体外壳（Framework）、智能体软件开发包（SDK）以及外挂系统（Harness）之间的边界^^。Harness 的底层使命是为单个智能体提供可运行的物理环境、硬性控制边界与结果核验设施，它处于多智能体框架（Framework，如 CrewAI 等负责角色路由和协作模式的系统）的下方^^。

为了避免在外挂层重复造轮子，导致不必要的运行开销或对内核原生优化机制的破坏，升级设计应当秉持“减法原则”，剔除所有与内核冲突的冗余设计，集中资源构建高价值的外部管控功能。

### 外挂系统功能必要性与冗余度判定

| **外挂功能模块**                    | **机制描述与设计意图**                                                                                                                          | **判定结论** | **深度冗余性分析与外挂层实施指南**                                                                                                                                                                                                                                               |
| ----------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **低层工具解析与协议格式化**        | 拦截智能体输出，手动提取工具名称，匹配 Schema 并转换为系统调用^^。                                                                                    | **彻底冗余** | **千万不要自研** 。Claude Agent SDK 已经深度绑定 Zod Schema，Codex App Server 的 JSON-RPC 自带解析器^^。外挂层强行介入不仅会显著增加运行延迟，还会由于自定义解析器对大模型不规则流式输出的处理不稳而导致会话频频崩溃^^。                                                         |
| **微观文本摘要与上下文裁剪**        | 在外挂层通过正则或调用其他廉价 LLM 对大模型历史对话进行文本级摘要压缩^^。                                                                             | **彻底冗余** | **严禁在外挂层执行** 。Claude Code 具有 5 阶紧缩管线，Codex 具有专有的服务端隐空间压缩 Blob^^。外挂层任何对 Prompt 结构的微调都会使 Codex 的前缀缓存失效，导致首字延迟（TTFT）激增，且文本抽象往往丢失关键的局部技术细节^^。                                                     |
| **单文件级快照备份与手动回滚**      | 在模型尝试编辑单个文件前，外挂在临时文件夹备份原文件，以便模型报错时还原^^。                                                                          | **部分冗余** | Claude Code 内核已天然提供这一能力（按两下 `Esc`回滚或命令回滚）^^。外挂系统应避免实现细粒度的物理备份，而应在更宏观的**版本控制级别（Git Worktree）**上建立安全屏障^^。                                                                                                             |
| **临时物理工作树隔离沙箱**          | 动态创建临时物理工作区（Ephemeral Worktrees），实现多个智能体任务在互不干扰的独立目录中并行演进^^。                                                   | **必须建设** | 智能体在工作区中直接运行危险脚本或执行不稳定的代码合并极易弄脏开发者的 Working Copy^^。外挂系统应当接管这一过程：在根目录之外使用 `git worktree add`创建完全隔离的物理路径^^，并在该隔离路径内启动 Claude Code 或 Codex，从源头上解决代码并发冲突^^。                                |
| **Plan-First 计划门控与审批流**     | 强制智能体在编码前必须先输出一份 `Todos/Plan`，在外挂层拦截该计划，由人类通过审批后再允许执行写操作^^。                                             | **必须建设** | 没有任何模型能够保证 100% 的意图对齐^^。如果允许智能体直接进入行动（Act）阶段，在大型项目上可能会引入高昂的重构成本^^。外挂系统应该在 `queryLoop`或 `App Server`的入口处将智能体初始化为 `Plan`模式，并设计带有回调（Callback）机制的阻断式 TUI / API 触发器，实现非对称控制^^。 |
| **爬山算法改进循环 (Level 4 Loop)** | 搜集每一次智能体执行留下的物理痕迹（Telemetry Traces），调用专项模型分析失败模式，自动化重写或修正 System Prompt、Tool Schema 或 Verifier Rubrics^^。 | **核心增值** | 静态的 Agent 配置往往在遇到新的长尾错误时就宣告失效。外挂系统作为包裹模型运行的“车身”，最适合承担这种元层面的自我进化^^。利用这一机制可以使外挂系统具备在生产环境中自我适应、自主修补配置文件的能力，不断拉高系统的 SWE-bench 解决率^^。                                             |

## 优雅落地：基于适配器模式的高内聚控制面架构

为了让升级后的外挂系统不仅能完美支持 Claude Code 和 OpenAI Codex，甚至能无缝兼容未来可能接入的全新编码后端（如 Cursor CLI 或 Gemini ADK）^^，外挂系统应当采用“单控制平面适配器架构（Single Control Plane Adapter Architecture）”^^。

该架构的设计精髓在于：将环境供给、流控安全、人机协同审批以及元级改进逻辑全部高聚拢在外部外挂控制面（Outer Harness），而将不同的智能体底层运行时作为可插拔的通道适配器（Channel Adapters）^^。

### 优雅外挂架构的核心运行链路

```
[任务流/定时/网络钩子]
         │
         v
┌────────────────────────────────────────────────────────┐
│             外部外挂控制面 (Outer Harness)              │
│                                                        │
│  1. 唤醒逻辑控制 (Cron / Heartbeat 调度引擎)           │
│  2. 工作空间准备 (Git Ephemeral Worktrees)             │
│  3. 任务生命周期状态机管理 (Active -> Pending -> PR)   │
│  4. 自适应改进层 (Level 4 Trace Telemetry Analyst)     │
└──────────────────────────┬─────────────────────────────┘
                           │
                           v
┌────────────────────────────────────────────────────────┐
│        标准后端适配层 (BaseAgentAdapter)               │
├──────────────────────────┬─────────────────────────────┤
│ 接口: initializeSession  │ 接口: submitPrompt           │
│ 接口: executeTurn        │ 接口: handleGatingPlan      │
└────────────┬─────────────┴──────────────┬──────────────┘
             │                            │
             v                            v
┌──────────────────────────┐┌────────────────────────────┐
│   Claude Code 适配器     ││    OpenAI Codex 适配器     │
├──────────────────────────┤├────────────────────────────┤
│ * 映射 Agent SDK 异步生成 ││ * 创建 stdio JSON-RPC 管道  │
│   器 (queryLoop)         ││ * 实例化 Thread / Turn 基元 │
│ * 加载 CLAUDE.md 指令层  ││ * 尾部追加 Prompt 优化对齐 │
│ * 代理侧链 JSONL 转录     ││ * 代理隐空间潜意识 Blob 压缩│
└──────────────────────────┘└────────────────────────────┘
```

为了确保该适配器架构在面对超长复杂任务时的稳定性和高性价比，外挂系统应当在控制面上实施以下两大优雅的设计模式。

### 1. 物理级“管理者-执行者”双层解耦协同（The Decoupled Manager-Worker Pattern）

当需要应对庞大且模糊的工程需求时，如果直接将任务投递给配备了各种读写和 Bash 权限的高能力智能体，由于目标过于宽泛，大模型往往会陷入长时间的低效乱撞^^。优雅的实践是设计两层解耦的自动化运行管道^^：

* **管理者循环 (Manager Loop)** ：通常配置为超高推理能力、大上下文容量但高成本的模型（如 Claude Fable）^^。管理者不直接操作文件，而是充当“架构规划师”和“项目主管”，主要负责分析 GitHub 上的 Issue，提取技术边界与架构准则，将庞大的系统目标拆解为粒度极细、无状态依赖且带有严格验证标准（Rubrics）的子任务 JSON 队列^^。
* **执行者循环 (Worker Loop)** ：通常选用执行速度快、成本更低的专业编码模型（如特化后的 Codex 或 Sonnet 4.5）^^。执行者由外挂的任务队列管理器（SQLite / Task Queue）调度，批量或并发地获取子任务^^。每个执行者被分派到一个独立的工作树隔离空间中，仅被赋予完成该特定原子任务所需的最小工具集（根据“减法原则”，避免工具过多造成的模型选择干扰）^^。

### 2. 双重确认完成合约（Two-Step Completion Contract）与 Verifier 独立节点

智能体在长期的自主编码、构建、报错和修补循环中，极易产生“认知盲区”，例如由于缺乏外部评测视角而反复对已经产生编译错误的相同代码块进行微调^^。为了保证交付质量，外挂系统应当在智能体宣称“任务已完成”后，强制介入一个完全解耦的独立验证阶段^^：

* **验证节点分离 (The Verifier Node)** ：将“编写者”（Maker）与“验证者”（Verifier）在模型角色甚至是 API Key 上彻底隔离开来^^。当执行者智能体触发任务完成信号时，外挂控制面并不直接发起合并^^。相反，它会在一个全新的、完全无状态的环境中，拉起一个只配备了只读与执行工具（如 `Read`、`npm test` 跑测器）的 Verifier 智能体节点^^。
* **基于评分量规的自适应反馈** ：验证节点不依赖简单的布尔值判断，而是基于外挂传入的、包含语法 Lint、性能基准和 CI 指标的 Rubrics 矩阵对生成的 Diff 进行全面评分^^。如果未达标，Verifier 将生成一份极其客观、剥离了所有情绪和啰嗦废话的结构化诊断报告（包含具体出错的代码片段和期望输出对比）^^。外挂系统会将此报告作为一个清晰的迭代 Turn 注入回执行者智能体的会话尾部，直到多次尝试均失败（触发外挂硬熔断条件）或顺利通过验证分数为契机，完成 PR 挂起与全流程的优雅收尾^^。

通过这种方式，升级后的 Harness 外挂系统既能享受到 Claude Code 和 OpenAI Codex Natively 提供的优秀底层工具和上下文紧缩技术，又通过隔离、门控和自适应监控将其约束在安全高效的企业级生产轨道上^^。
